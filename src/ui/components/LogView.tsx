import React, {ReactElement} from "react"

/*
 * Display Log messages that would be seen in console, these can be written to a file as well
 */

export default function LogView(): ReactElement {
    return
}